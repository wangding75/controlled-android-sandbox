# P2-03 Quark acceptance — 2026-09-11

## Result

`P2-03=BLOCKED_QUARK_WEBVIEW_ANR`.

`QUARK_FIRST_FRAME=PASS` and `QUARK_SERVICE_PROVIDER=PASS`. The browser BASIC_SMOKE is
not closed: after a normal device-side unlock state was already in effect, the real Host
entry reached Quark's privacy page, then the imported Quark process hit a reproducible
UC WebView/Chromium input-dispatch ANR. No App Lock configuration was changed or bypassed.

## Frozen coordinates

| Item | Value |
|---|---|
| Device | Xiaomi `25019PNF3C` / `xuanyuan`, Android 16 / API 36 |
| ABI / page size | `arm64-v8a` / 4096 bytes |
| Quark | `com.quark.browser` `10.15.5.1130` / code `1130` |
| Quark APK SHA-256 | `81bddb678f3918b683cdc48c0ccf8682137e0c1cbf23930df17b36120446206a` |
| Host APK | `0.5.19.1-source-debug` / code `19` |
| Host APK SHA-256 | `9eeb759d9a54ae02012c18209cdcaed612735fdfc63ccbef752a7a3647f713f1` |

## First error and root cause

The first valid Quark hold showed `u4ashmem ... fstat ... EACCES`, followed by native
recursive initialization and Guest abort. A `memfd_create` admission attempt was kept as
the first failed fix and reverted after targeted regression. Device `readelf` then proved
that `libwebviewuc.so` imports both `fstat` and `fstat64`; the `fstat64` target was added,
but the targeted rerun still failed because Android 16 public `/dev/ashmem<uuid>`
descriptors can arrive before CAS ledger registration.

The final narrow fix adds `fstat64` to the same CAS PLT/replacement path and admits only an
unregistered FD whose trusted `/proc/self/fd/<n>` target is exactly `/dev/ashmem` or one
kernel-named ashmem instance. HostInternal, Broker, stale-revision, and all other unknown
FDs remain fail-closed. No NBB/VA Service/Provider route or Quark-specific branch changed.

## Targeted regression

- `import-prepare`: `PREPARED_DEGRADED`; Provider preparation succeeded.
- Native hook: `patched=206`, `patchFailures=0`.
- 3 cold + 3 hot `hold-prepare` runs: 6/6 internal first-frame markers, 6/6 Provider/Service
  markers, 0 new ashmem EACCES, 0 FATAL/ANR.
- The UI hierarchy during a live hold was owned by
  `com.miui.securitycenter/.applicationlock.AppLockActivity` and displayed fingerprint/password
  unlock controls. This historical first UI condition is retained as evidence; later normal
  device-side unlock state allowed the Host entry to reach Quark without App Lock. No App Lock
  configuration was changed.

## 2026-09-11 continuation: harness first error and real UI recheck

The first `component-suite` run after the CAS Native fix was request
`658eab97-1d47-407d-aba3-a478d7a90295`. Service, receiver, and provider substeps succeeded,
but the debug harness rejected the runtime's valid `ALREADY_PREPARED_DEGRADED` terminal state
with `prepare failed: status=ALREADY_PREPARED_DEGRADED`. Before changing it, CAS lifecycle
semantics, NBB/VA's host-owner/virtual-component lifecycle, and Android's valid degraded
readiness behavior were checked. The only change was to accept
`PREPARED_DEGRADED`/`ALREADY_PREPARED_DEGRADED` in
`app/src/debug/java/com/warden/controlledsandbox/DebugCommandActivity.java`.

The targeted regression used the same device, Quark revision and explicit trust admission,
request `e5c7c86b-0551-4293-a386-eb496203bfd2`:

- `status=PASS`, operation `ALREADY_PREPARED_DEGRADED`;
- Service `SERVICE_STARTED` (`com.amap.api.location.APSService`);
- receiver `BROADCAST_DELIVERED` (`com.ucpro.services.download.broadcast.DownloadBroadcastReceiver`);
- Provider `PROVIDER_ALREADY_READY` (`androidx.core.content.FileProvider`);
- no `NO_GUEST_SERVICE_MATCH`, authority virtualization failure, FATAL, or new ashmem error.

The rebuilt Host APK installed on the device has SHA-256
`9eeb759d9a54ae02012c18209cdcaed612735fdfc63ccbef752a7a3647f713f1`.

The real Host UI run was performed without `uiautomator` (screenshots and WMS only). Quark
reached the privacy page in [host-launch-4s.png](../../out/verification/p2-03-quark-20260911/host-launch-4s.png).
WMS showed a Host `StubActivity60/60W1` ActivityRecord with a Guest-named
`com.quark.browser/com.ucpro.BrowserActivity` window and Host `LayoutParams.package`; this is
the expected CAS WindowManager projection and matches the NBB/VA proxy/stub model. The Xiaomi
task state reported `mBehindAppLockPkg=null`.

The remaining first error is retained in
[clean-consent-logcat.txt](../../out/verification/p2-03-quark-20260911/clean-consent-logcat.txt):
after `GUEST_ACTIVITY_FIRST_FRAME_DRAWN`, Quark's U4 path logged a `WebCoreManager` NPE and a
missing `nativeSetEnabledMremap` implementation, then `loadDataWithBaseURL` appeared in the
Chromium stack and the process received `receive ANR signal`. Android's ANR record also points
into the frozen revision's `libunet.so`/Quark native payload. No CAS Service/Provider miss,
Host-identity leak, or App Lock activity was observed in this run. Because the evidence does
not identify a CAS/NBB/VA contract defect, no guessed WebView or package-specific workaround
was applied.

## Acceptance ledger

| Gate | Result | Evidence |
|---|---|---|
| Xiaomi/fingerprint/API/ABI/page size | PASS | P2-01 baseline and 2026-09-11 recheck |
| Quark sample/version/hash | PASS | frozen package metadata above |
| CAS first frame | PASS | 6/6 `GUEST_ACTIVITY_FIRST_FRAME_DRAWN` |
| Service/Provider | PASS | `GUEST_PROVIDERS_BOUND`, `GUEST_SERVICE_FRAMEWORK_STARTED` |
| Browser navigation/search/input/tabs/back | BLOCKED_QUARK_WEBVIEW_ANR | U4/Chromium native path ANR after first frame; no CAS route error |
| File picker/test download | NOT RUN | BASIC_SMOKE is blocked before this step |
| P2-90 / P2-91 | NOT RUN | taskbook ordering |
