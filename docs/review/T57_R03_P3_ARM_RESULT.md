# T57-R03-P3 ARM runtime

RESULT: `UNVERIFIED_RUNTIME`

The execution host is Windows AMD64. An `arm64_test.avd` exists in the
user AVD directory; that is not accepted as native ARM64 evidence.

No physical ARM64 phone or cloud ARM64 device was attached.

APKs contain `arm64-v8a` / `armeabi-v7a` libraries. Compilation and
packaging are not ARM64 dynamic PASS.

ARM32: `UNVERIFIED_RUNTIME`.
