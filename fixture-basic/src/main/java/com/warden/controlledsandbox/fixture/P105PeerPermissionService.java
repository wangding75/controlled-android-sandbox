package com.warden.controlledsandbox.fixture;

/**
 * Cross-package P1-05 fixture.  The declaration, rather than this implementation, carries the
 * signature-level access boundary so the probe can distinguish a permission denial from absence.
 */
public final class P105PeerPermissionService extends FixtureService { }
