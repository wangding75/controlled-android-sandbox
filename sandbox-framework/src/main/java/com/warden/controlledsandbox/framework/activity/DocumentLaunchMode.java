package com.warden.controlledsandbox.framework.activity;

/** Manifest-compatible document launch policy used by the virtual task model. */
public enum DocumentLaunchMode {
    NONE,
    INTO_EXISTING,
    ALWAYS,
    NEVER
}
