C4-TEMP-01 independent diagnostic setup failure (2026-08-30 03:29:59 +08:00)

This was a pre-test harness failure. No valid product request/operation was emitted.

Command outcome:
- `from scripts.mumu_instance import resolve_rd_environment` -> ImportError: the
  resolver is the `run_rd_campaign.resolve_rd_environment` wrapper, not an export
  from `scripts/mumu_instance.py`.
- The empty serial then caused `adb.exe: unknown command -c`; the background
  logcat process was stopped immediately and captured 0 bytes.
- `run_c4_temp_01_quark_latency.py --samples 1` -> `--samples must be at least 3`.

Classification: harness/setup error only; not a CAS or Quark product attempt,
not an automatic retry, and not used as the C4-TEMP-01 acceptance result.
